{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Your starting portfolio, to iterate on.",
  "main": "index.js",
  "scripts": {
    "buildStyles": "npx tailwindcss -i ./input.css -o ./public/output.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.3.3"
  }
}
